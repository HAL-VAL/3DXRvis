httpとhttpsの違いと、各デバイス（PCかスマホ）でアクセスする時の違い

Hopperとada Lovelaceは現在のGPUの基礎というか、
同じ？アーキテクチャ

1980？年のGPUは、GPUとして出来上がってるけど、
携帯には使用できない

GPUの容量？が、144？必要なのは、
言語モデルの発展にある？

アニメーションの生成で、
例えばボールが落ちた時の周りの影響がどのように表現されるか？
空気が揺れたり地面にどのように影響がでるのか？（波の表現）
を書く。書き方。テスト内容の1つ

mesh.scale.set(2, 2, 2)

自転しながら公転
mesh.position.x = Math.cos(time);
mesh.position.y = Math.cos(time);

camera.far = 3000

polaの値の変更

OrbitControl(camera)

render関数のところで、controls.update()をしないと
動かない

MeshNomalMaterial({...})

fratShading: true

triange
ポリゴンの三角形c1-c3までの頂点がある時、
Δ=c1+c2+c3/3

flatShading: falseの時
よく見ると、ガタガタな表面がある→ポリゴンを増やす

shininess

wireframe

transparent:true, opacity: 0.5

TextureLoader
nomalMap
specMap

DirectionalLight [can change color]

PointLight

SpotLight

penumbra

AmbientLight




